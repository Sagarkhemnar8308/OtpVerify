const express= require('express');
const server=express.Router();

server.get('/getalluser',()=>{});

server.post('/signup',(req,resp)=>{});

server.put('/updateuser',(req,resp)=>{});

server.delete('/deleteuser',(req,resp)=>{});

module.exports=server;